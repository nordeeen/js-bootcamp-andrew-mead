// 1. You can't define a variable more than once.
let petName = 'Kurama';
petName = 'Kurma';
console.log(petName);

// 2. There are rules related to variables names
let test_ = 2;
let result = 3 + 4;

// 3. Variables names cannot be reselved keywords
// let let = 13;
