// === - equality operator (operator persamaan)
// !== - no equal operator (operator tidak sama dengan)
// < - less than operator (operator perbandingan lebih kecil dari )
// > - greater than operator (operator perbandingan lebih besar dari )
// <= - less than or equal to operator (operator perbandingan lebih kecil dari atau sama dengan )
// >= - greater than or equal to operator (operator perbandingan lebih besar dari atau sama dengan )

let temp = 35;
let isFreezing = temp >= 35;
console.log(isFreezing);

// Challange Area

// Create age set to your age
// Calculate isChild - if they are 7 or under
// Calculate isSenior - if they are 65 or under
// print is Child value
// print is Senior value
let age = 10;
let isChild = age <= 7;
let isSenior = age >= 65;

console.log(isChild);
console.log(isSenior);
